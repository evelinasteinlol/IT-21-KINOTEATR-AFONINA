#  Информационная система: [Кинотеатр]
> **Предметная область:** [Кинотеатр]
> **Разработчик:** Группа [21ИС], [Афонина Елизавета], [Козина Кристина], [Штейн Эвелина], [Гэу анатолий]

## 1. Описание проекта
Данный проект представляет собой прототип информационной системы (ИС) для автоматизации [работы Кинотеатра]. 
Система позволяет структурировать хранение данных и быстро получать аналитическую информацию через SQL-запросы.

##  2. Техническое задание (ТЗ) вкратце
*   **Цель:** Перейти от бумажного учета к автоматизированной базе данных на SQLite.
*   **Основные функции:**
    1. Регистрация новых записей (клиенты, товары, услуги).
    2. Поиск данных по заданным критериям.
    3. Формирование итоговых отчетов за период.
*   **Требования к БД:** Минимум 4 связанные таблицы, соблюдение целостности данных.

##  3. Структура базы данных (ER-схема)
Проект базируется на следующих сущностях:
1.  **[Cinema-Halls]** — хранит данные о [Статусах залов кинотеатра].
2.  **[Movies]** — хранит данные о [Фильмах]
3.  **[Tickets]** — отвечает за [Печать билетных чеков на определённые сеансы].
4.  **[Sessions]** — фиксирует связи между [Cinema_Halls и Movies].

*Скриншот ER-диаграммы находится в папке `/diagrams`.*

##  4. Реализованные SQL-запросы
В рамках проекта подготовлены сценарии для:
*   Подсчета общего количества [товаров/услуг].
*   Выборки данных с использованием `JOIN` (объединение таблиц).
*   Фильтрации данных по условию (например, цена > 1000).

##  Структура репозитория
*   `/data` — файл базы данных `project.db`.
*   `/sql` — скрипты создания таблиц (`create.sql`) и тестовые данные (`insert.sql`).
*   `/diagrams` — графическая схема базы данных.
*   `queries.sql` — файл с примерами сложных запросов.
*   `main.py` — точка входа в программу. Здесь обычно располагается основной код, который запускает приложение. Можно описать, что main.py служит «дирижёром», который соединяет все компоненты вместе.
*   `db.py` — файл, посвящённый работе с базой данных. В отчёте можно указать, какие функции или классы используются для взаимодействия с БД, например, подключение к ней, выполнение запросов, обработка результатов.
*   `reports.py` — файл, связанный с отчётами. Здесь можно описать, какие отчёты создаются, как они формируются и какие данные используются.
*   `services.py` — модуль, который может содержать бизнес-логику или вспомогательные функции, не связанные напрямую с базой данных или отчётами.

##  Как запустить (Инструкция)
1. Установите **DB Browser for SQLite**.
2. Склонируйте репозиторий: `git clone [ссылка на ваш репозиторий]`.
3. Откройте файл из папки `/data` и выполните запросы из файла `queries.sql`.

---
*Работа выполнена в рамках учебной дисциплины Основы проектирования БД.*
